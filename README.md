it is a Tumor detector , where user will uplaod it's MRI scan image .
based on it model will tell which type of tumor it has 
basic types are Glioma, Meningioma, No tumor, Pituitary . 
in the backend file i have laod the model which i finetune using dataset from kaggle .
it's size is large so it cannot be uplaod here.
if anyone need it then you need to run tumor_detection.py file to train the model again 
